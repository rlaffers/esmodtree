{
  "name": "nvim-esmodtree",
  "private": true,
  "scripts": {
    "test": "make test",
    "format:check": "stylua --check lua/",
    "docgen": "mkdir -p doc && ./panvimdoc.sh --project-name esmodtree --input-file README.md --description 'Neovim plugin for visualizing ECMAScript module import trees.' --vim-version 'Neovim >= 0.10.0' --toc true --demojify true --treesitter true --doc-mapping true"
  },
  "dependencies": {
    "@esmodtree/cli": "0.1.1"
  }
}
