{
  "name": "@esmodtree/cli",
  "version": "1.1.0",
  "description": "ES module import tree visualizer for the command line",
  "type": "module",
  "bin": {
    "esmodtree": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build",
    "prepack": "pnpm build",
    "prepublishOnly": "pnpm check",
    "format": "pnpm exec prettier . --write",
    "format:check": "pnpm exec prettier . --check",
    "typecheck": "tsc -p tsconfig.json",
    "typecheck:ci": "tsc -p tsconfig.json --pretty false",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "pnpm run format:check && pnpm run typecheck:ci && pnpm run test"
  },
  "keywords": [
    "dependency-tree",
    "import-tree",
    "typescript",
    "cli"
  ],
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "vite-plugin-dts": "^4.5.4"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "dependency-cruiser": "^17.3.10",
    "picocolors": "^1.1.1"
  }
}
